// Update.tsx
import { useParams } from 'react-router-dom';

function Update() {
  const { id } = useParams();

  // Implemente a lógica para atualizar um dado pelo ID aqui
  return <div>Update Component</div>;
}

export default Update;